use device_query::Keycode;
use enigo::{Key, KeyboardControllable};
use toca::display::KeyboardPlayer;
use toca::record::{KeyboardAction, KeyboardEv};
use toca::set_timeout;

#[allow(unused)]
fn main() {
    println!("nothing here, see tests/examples.");
}
